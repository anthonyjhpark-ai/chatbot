# 🏀 NBA 선수 스탯 대시보드 - 빠른 시작 가이드

## ✅ 프로젝트 설정 완료!

Next.js와 Supabase를 사용한 NBA 선수 일일 스탯 수집 및 종합 점수 랭킹 시스템이 성공적으로 생성되었습니다.

---

## 📋 다음 단계

### 1️⃣ 환경 변수 설정

`.env.local` 파일을 생성하고 설정하세요:

```bash
# .env.local 파일 생성
cp .env.example .env.local
```

다음 값을 입력하세요:
```env
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
```

### 2️⃣ Supabase 데이터베이스 설정

1. [Supabase](https://supabase.com)에 로그인
2. 새 프로젝트 생성
3. SQL Editor 열기
4. `supabase/nba-schema.sql` 파일의 내용을 복사하여 실행

### 3️⃣ 개발 서버 실행

```bash
npm run dev
```

브라우저에서 http://localhost:3000 접속

---

## 🎯 주요 기능

### ✨ 구현된 기능

- ✅ **일일 스탯 수집**: NBA API에서 자동으로 스탯 수집
- ✅ **커스텀 가중치**: 사용자가 정의한 가중치로 점수 계산
- ✅ **실시간 랭킹**: 날짜별 선수 랭킹 조회
- ✅ **다크 모드 UI**: 깔끔하고 모던한 대시보드
- ✅ **반응형 디자인**: 모든 디바이스 지원
- ✅ **4가지 가중치 프리셋**: 기본, 공격 중심, 수비 중심, 플레이메이커

### 📊 대시보드 구성

1. **통계 카드**: 전체 선수 수, 평균 점수, 최고 득점자
2. **필터 섹션**: 날짜 선택, 가중치 선택
3. **랭킹 테이블**: 선수별 종합 점수 순위

---

## 🗂 프로젝트 구조

```
CHATBOT/
├── app/
│   ├── api/                 # API Routes
│   │   ├── stats/          # 스탯 수집 & 계산
│   │   ├── rankings/       # 랭킹 조회
│   │   └── weights/        # 가중치 관리
│   ├── layout.tsx          # 레이아웃
│   ├── page.tsx            # 메인 대시보드
│   └── globals.css         # 글로벌 스타일
│
├── components/             # React 컴포넌트
│   ├── RankingTable.tsx   # 랭킹 테이블
│   ├── StatCard.tsx       # 통계 카드
│   ├── WeightSelector.tsx # 가중치 선택기
│   └── DateSelector.tsx   # 날짜 선택기
│
├── lib/                   # 유틸리티
│   ├── nba-api.ts        # NBA API 연동
│   ├── scoring.ts        # 점수 계산
│   └── supabase.ts       # Supabase 클라이언트
│
├── types/                # TypeScript 타입
│   └── nba.ts           # NBA 관련 타입
│
└── supabase/            # 데이터베이스
    └── nba-schema.sql   # 스키마 정의
```

자세한 내용은 `PROJECT_STRUCTURE.md` 참조

---

## 🔌 NBA API 연동

### 추천 API 서비스

1. **Ball Don't Lie API** (무료, 추천)
   - URL: https://www.balldontlie.io/
   - 키 불필요
   - 설정: `lib/nba-api.ts` 수정 필요

2. **SportsData.io** (프로페셔널)
   - URL: https://sportsdata.io/
   - 월 1,000 무료 요청

3. **RapidAPI NBA API**
   - URL: https://rapidapi.com/

### API 연동 방법

`lib/nba-api.ts` 파일에서 선택한 API에 맞게 함수를 수정하세요.

자세한 가이드는 `SETUP_GUIDE.md` 참조

---

## 📚 문서

- **README.md**: 프로젝트 개요 및 기능 소개
- **SETUP_GUIDE.md**: 상세 설정 가이드
- **PROJECT_STRUCTURE.md**: 프로젝트 구조 설명
- **이 문서 (QUICKSTART.md)**: 빠른 시작 가이드

---

## 🎨 화면 구성

### 메인 대시보드 (`/`)

```
┌─────────────────────────────────────────────────┐
│  🏀 NBA 선수 스탯 대시보드         [스탯 수집]   │
├─────────────────────────────────────────────────┤
│                                                 │
│  📊 통계 카드                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │전체 선수 │ │평균 점수 │ │최고득점자│       │
│  └──────────┘ └──────────┘ └──────────┘       │
│                                                 │
│  🔧 필터 설정                                   │
│  ┌─────────────────┐ ┌─────────────────┐      │
│  │ 날짜 선택       │ │ 가중치 선택     │      │
│  └─────────────────┘ └─────────────────┘      │
│                                                 │
│  🏆 선수 랭킹                                   │
│  ┌─────────────────────────────────────┐      │
│  │ 순위 │ 선수   │ 팀  │ 포지션 │ 점수 │      │
│  ├─────────────────────────────────────┤      │
│  │  🥇  │ 선수1  │ LAL │   F    │ 85.5 │      │
│  │  🥈  │ 선수2  │ GSW │   G    │ 82.3 │      │
│  │  🥉  │ 선수3  │ BOS │   F    │ 79.1 │      │
│  │  4   │ 선수4  │ MIA │   C    │ 76.8 │      │
│  └─────────────────────────────────────┘      │
└─────────────────────────────────────────────────┘
```

---

## 🚀 사용 방법

### 스탯 수집

1. 대시보드 상단의 **"스탯 수집"** 버튼 클릭
2. NBA API에서 오늘의 경기 스탯을 가져와 저장
3. 자동으로 점수 계산
4. 랭킹 테이블에 표시

### 가중치 변경

1. **"가중치 선택"** 드롭다운 클릭
2. 원하는 가중치 선택 (기본, 공격 중심, 수비 중심, 플레이메이커)
3. **"편집"** 버튼으로 가중치 상세 정보 확인
4. 랭킹이 자동으로 업데이트됨

### 날짜 변경

1. 날짜 선택 입력 사용
2. 또는 ← → 버튼으로 이전/다음 날로 이동
3. 선택한 날짜의 랭킹 조회

---

## 🔄 자동화 (선택사항)

### Vercel Cron Jobs

`vercel.json` 파일이 이미 설정되어 있습니다:

- **매일 12:00**: 스탯 자동 수집
- **매일 12:30**: 점수 자동 계산

Vercel에 배포하면 자동으로 활성화됩니다.

---

## 🐛 문제 해결

### Supabase 연결 실패
- `.env.local` 파일 확인
- 환경 변수명 확인 (`NEXT_PUBLIC_` 접두사)
- 개발 서버 재시작

### API 호출 실패
- API 키 확인
- 네트워크 연결 확인
- 브라우저 콘솔 확인

### 데이터가 표시되지 않음
- Supabase에서 직접 쿼리 실행해보기
- RLS 정책 확인

자세한 문제 해결은 `TROUBLESHOOTING.md` 참조

---

## 💡 다음 단계

### 즉시 시작
1. ✅ 환경 변수 설정
2. ✅ Supabase 스키마 실행
3. ✅ `npm run dev` 실행
4. ✅ http://localhost:3000 접속

### NBA API 연동
1. API 서비스 선택 (Ball Don't Lie 추천)
2. `lib/nba-api.ts` 수정
3. 테스트 실행

### 배포
1. GitHub에 푸시
2. Vercel에 연결
3. 환경 변수 설정
4. 배포 완료!

---

## 📞 지원

문제가 발생하면 다음 문서를 참조하세요:
- `SETUP_GUIDE.md`: 상세 설정 가이드
- `PROJECT_STRUCTURE.md`: 코드 구조
- `TROUBLESHOOTING.md`: 문제 해결

---

**Happy Coding! 🏀**
